algoritmo "EX043"
var
   contador: Inteiro
inicio
   contador <- 30
   Enquanto (contador >= 1) faca
      Escreva(contador)
      contador <- contador - 1
   FimEnquanto
   EscrevaL()
fimalgoritmo